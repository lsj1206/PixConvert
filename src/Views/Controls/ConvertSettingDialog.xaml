<UserControl x:Class="PixConvert.Views.Controls.ConvertSettingDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             mc:Ignorable="d" MinWidth="350">

    <StackPanel Orientation="Vertical" Margin="0,10,0,0">
        <!-- 대상 포맷 지정 -->
        <TextBlock Text="{DynamicResource Setting_TargetFormat}" FontWeight="SemiBold" Margin="0,0,0,8"/>
        <ComboBox ItemsSource="{Binding SupportedExtensions}"
                  SelectedItem="{Binding TargetExtension}"
                  HorizontalAlignment="Stretch" Margin="0,0,0,16"/>

        <!-- 품질 조절 -->
        <TextBlock Text="{DynamicResource Setting_Quality}" FontWeight="SemiBold" Margin="0,0,0,8"/>
        <Grid Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Slider Value="{Binding Quality}" Minimum="1" Maximum="100"
                    SmallChange="1" LargeChange="10" IsSnapToTickEnabled="True" TickFrequency="1"
                    VerticalAlignment="Center" Margin="0,0,12,0"/>
            <TextBlock Grid.Column="1" Text="{Binding Quality}" VerticalAlignment="Center" Width="30" TextAlignment="Right"/>
        </Grid>

        <!-- 덮어쓰기 옵션 -->
        <CheckBox Content="{DynamicResource Setting_Overwrite}" IsChecked="{Binding Overwrite}"/>
    </StackPanel>
</UserControl>
